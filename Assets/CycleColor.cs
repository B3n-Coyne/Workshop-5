using System.Linq;
using UnityEngine;

[RequireComponent(typeof(MeshFilter))]
[RequireComponent(typeof(MeshRenderer))]
public class CycleColor : MonoBehaviour
{
    // Color HSVTToRGB(Float H, float S, float V);
    // Hue, saturation, brightness [0..1]

}